# ═══════════════════════════════════════════════════════════════
#  ForgeX 依赖清单 — RTX 5060 Blackwell (sm_120 / cu128)
# ═══════════════════════════════════════════════════════════════
#
#  安装方法 (二选一):
#
#  方法 A — 一键脚本 (推荐):
#    chmod +x install_forgex.sh && ./install_forgex.sh
#
#  方法 B — 手动分步:
#    1) 先装 PyTorch:
#       pip install torch==2.10.0 torchvision==0.25.0 torchaudio==2.10.0 --index-url https://download.pytorch.org/whl/cu128
#    2) 再装其他:
#       pip install -r requirements.txt
#
#  ⚠️ 必须先装 PyTorch cu128 再装本文件, 否则 pip 会拉到 CPU 版本!
#  ⚠️ 不要安装 xformers / flash-attn (Blackwell 不兼容, 代码已用 SDPA)
# ═══════════════════════════════════════════════════════════════
transformers>=4.48.0,<5.0.0
# ━━━ 核心训练栈 ━━━

datasets>=3.0.0
peft>=0.14.0
trl>=0.12.0
accelerate>=1.0.0
huggingface_hub>=0.25.0
safetensors>=0.4.0
tokenizers>=0.20.0

# ━━━ UI ━━━
gradio==4.44.1
gradio-client==1.3.0
fastapi==0.112.4
starlette==0.38.6

# ━━━ 基础工具 ━━━
numpy
scipy
pandas
PyYAML
requests
Pillow

# ━━━ 可选: QLoRA 量化 ━━━
bitsandbytes>=0.44.0

# ━━━ 可选: GGUF 导出 & 推理 ━━━
gguf>=0.10.0
# llama-cpp-python>=0.3.0  # 建议单独安装: CMAKE_ARGS="-DGGML_CUDA=on" pip install llama-cpp-python

# ━━━ 可选: 文档解析 ━━━
PyMuPDF>=1.24.0
pdfplumber>=0.11.0
python-docx>=1.1.0

# ━━━ 可选: 多模态音频 ━━━
soundfile>=0.12.0
librosa>=0.10.0

# ━━━ 可选: API 教师蒸馏 ━━━
openai>=1.30.0
